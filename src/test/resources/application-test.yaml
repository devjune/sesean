spring:
  datasource:
    url: jdbc:sqlite:data/database-test.sqlite
    driver-class-name: org.sqlite.JDBC
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.community.dialect.SQLiteDialect